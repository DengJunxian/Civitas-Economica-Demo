[pytest]
asyncio_mode = auto
addopts = --basetemp=.pytest_tmp
testpaths = tests
norecursedirs = tmp .tmp .pytest_tmp .pytest_tmp_check build dist
markers =
    unit: fast unit-level checks
    integration: multi-module integration checks
    regression: non-functional regression coverage
    determinism: reproducibility and deterministic replay checks

